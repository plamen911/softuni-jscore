{{> header}}
<div class="content">
    {{> menu }}
    <section id="viewCatalog">
        <div class="posts">
            {{#each posts}}
                {{> post}}
            {{else}}
                No posts in database
            {{/each}}
        </div>
    </section>
</div>
{{> footer}}