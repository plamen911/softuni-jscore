'use strict';
// https://judge.softuni.bg/Contests/Compete/Index/288#3

function calcCircleArea(radius) {
    let area = Math.PI * Math.pow(radius, 2);
    console.log(area);
    console.log(area.toFixed(2));
}

// calcCircleArea(5);